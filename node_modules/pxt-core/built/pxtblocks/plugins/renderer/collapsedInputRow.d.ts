import * as Blockly from "blockly";
import { ConstantProvider } from "./constants";
export declare class CollapsedInputRow extends Blockly.blockRendering.InputRow {
    constructor(constants: ConstantProvider);
    measure(): void;
}
export declare function isCollapsedInputRow(row: Blockly.blockRendering.Row): row is CollapsedInputRow;
