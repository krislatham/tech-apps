import * as Blockly from "blockly";
import { WorkspaceSearch } from "@blockly/plugin-workspace-search";
export declare class PxtWorkspaceSearch extends WorkspaceSearch {
    protected injectionDiv: Element;
    protected inputElement_: HTMLInputElement;
    constructor(workspace: Blockly.WorkspaceSvg);
    protected highlightSearchGroup(blocks: Blockly.BlockSvg[]): void;
    protected unhighlightSearchGroup(blocks: Blockly.BlockSvg[]): void;
    open(): void;
    close(): void;
}
